
from ui.app import Application
from petri import PetriNet

if __name__ == "__main__":
	# testing
	#net = PetriNet()
	#print(net)
	#net.addArrowPlaceToTransition(net.addPlace("p", 1, 2), net.addTransition("t", 2, 1))
	#print(net)
	#net.removeArrowPlaceToTransition(0, 0)
	#print(net)
	#net.removePlace(0)
	#net.removeTransition(0)
	#print(net)

	app = Application()
	app.start()
